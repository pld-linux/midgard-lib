diff -urN midgard-lib-1.4.1_5/Makefile.in midgard-lib-1.4.1_5.grzegorz/Makefile.in
--- midgard-lib-1.4.1_5/Makefile.in	Sat Apr 14 16:43:30 2001
+++ midgard-lib-1.4.1_5.grzegorz/Makefile.in	Wed Jun 20 12:51:13 2001
@@ -85,13 +85,11 @@
 
 EXTRA_DIST = README.ru INSTALL.ru
 ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
-mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
+#mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_CLEAN_FILES = 
 DIST_COMMON =  README AUTHORS COPYING ChangeLog INSTALL Makefile.am \
 Makefile.in NEWS acinclude.m4 aclocal.m4 config.guess config.sub \
-configure configure.in install-sh ltconfig ltmain.sh missing \
-mkinstalldirs
-
+configure configure.in install-sh ltconfig ltmain.sh missing
 
 DISTFILES = $(DIST_COMMON) $(SOURCES) $(HEADERS) $(TEXINFOS) $(EXTRA_DIST)
 
diff -urN midgard-lib-1.4.1_5/midgard/Makefile.in midgard-lib-1.4.1_5.grzegorz/midgard/Makefile.in
--- midgard-lib-1.4.1_5/midgard/Makefile.in	Sat Apr 14 16:43:30 2001
+++ midgard-lib-1.4.1_5.grzegorz/midgard/Makefile.in	Wed Jun 20 12:53:25 2001
@@ -84,7 +84,7 @@
 pkginclude_HEADERS = apache.h select_db.h tablenames.h midgard.h
 
 CLEANFILES = tablenames.h
-mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
+#mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_CLEAN_FILES =  midgard.h
 HEADERS =  $(pkginclude_HEADERS)
 
@@ -109,7 +109,7 @@
 
 install-pkgincludeHEADERS: $(pkginclude_HEADERS)
 	@$(NORMAL_INSTALL)
-	$(mkinstalldirs) $(DESTDIR)$(pkgincludedir)
+#	$(mkinstalldirs) $(DESTDIR)$(pkgincludedir)
 	@list='$(pkginclude_HEADERS)'; for p in $$list; do \
 	  if test -f "$$p"; then d= ; else d="$(srcdir)/"; fi; \
 	  echo " $(INSTALL_DATA) $$d$$p $(DESTDIR)$(pkgincludedir)/$$p"; \
@@ -195,7 +195,7 @@
 install-strip:
 	$(MAKE) $(AM_MAKEFLAGS) AM_INSTALL_PROGRAM_FLAGS=-s install
 installdirs:
-	$(mkinstalldirs)  $(DESTDIR)$(pkgincludedir)
+#	$(mkinstalldirs)  $(DESTDIR)$(pkgincludedir)
 
 
 mostlyclean-generic:
diff -urN midgard-lib-1.4.1_5/repligard/Makefile.in midgard-lib-1.4.1_5.grzegorz/repligard/Makefile.in
--- midgard-lib-1.4.1_5/repligard/Makefile.in	Sat Apr 14 16:43:30 2001
+++ midgard-lib-1.4.1_5.grzegorz/repligard/Makefile.in	Wed Jun 20 12:53:53 2001
@@ -93,7 +93,7 @@
 BUILT_SOURCES = main.c   
 EXTRA_DIST = AUTHORS.Repligard NEWS.Repligard README.Repligard ChangeLog.Repligard
 AM_INSTALL_PROGRAM_FLAGS = -m 4555 -o @REPLIGARD_OWNER@
-mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
+#mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_CLEAN_FILES =  repligard.conf main.c
 PROGRAMS =  $(bin_PROGRAMS)
 
@@ -153,7 +153,7 @@
 
 install-binPROGRAMS: $(bin_PROGRAMS)
 	@$(NORMAL_INSTALL)
-	$(mkinstalldirs) $(DESTDIR)$(bindir)
+#	$(mkinstalldirs) $(DESTDIR)$(bindir)
 	@list='$(bin_PROGRAMS)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    echo " $(LIBTOOL)  --mode=install $(INSTALL_PROGRAM) $$p $(DESTDIR)$(bindir)/`echo $$p|sed 's/$(EXEEXT)$$//'|sed '$(transform)'|sed 's/$$/$(EXEEXT)/'`"; \
@@ -205,7 +205,7 @@
 
 install-pkgdataDATA: $(pkgdata_DATA)
 	@$(NORMAL_INSTALL)
-	$(mkinstalldirs) $(DESTDIR)$(pkgdatadir)
+#	$(mkinstalldirs) $(DESTDIR)$(pkgdatadir)
 	@list='$(pkgdata_DATA)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    echo " $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pkgdatadir)/$$p"; \
@@ -224,7 +224,7 @@
 
 install-sysconfDATA: $(sysconf_DATA)
 	@$(NORMAL_INSTALL)
-	$(mkinstalldirs) $(DESTDIR)$(sysconfdir)
+#	$(mkinstalldirs) $(DESTDIR)$(sysconfdir)
 	@list='$(sysconf_DATA)'; for p in $$list; do \
 	  if test -f $(srcdir)/$$p; then \
 	    echo " $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(sysconfdir)/$$p"; \
@@ -347,7 +347,7 @@
 install-strip:
 	$(MAKE) $(AM_MAKEFLAGS) AM_INSTALL_PROGRAM_FLAGS=-s install
 installdirs:
-	$(mkinstalldirs)  $(DESTDIR)$(bindir) $(DESTDIR)$(pkgdatadir) \
+#	$(mkinstalldirs)  $(DESTDIR)$(bindir) $(DESTDIR)$(pkgdatadir) \
 		$(DESTDIR)$(sysconfdir)
 
 
diff -urN midgard-lib-1.4.1_5/src/Makefile.in midgard-lib-1.4.1_5.grzegorz/src/Makefile.in
--- midgard-lib-1.4.1_5/src/Makefile.in	Sat Apr 14 16:43:30 2001
+++ midgard-lib-1.4.1_5.grzegorz/src/Makefile.in	Wed Jun 20 12:54:13 2001
@@ -89,7 +89,7 @@
 
 
 CLEANFILES = tablenames.c
-mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
+#mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_CLEAN_FILES =  iconvxml.c
 LTLIBRARIES =  $(lib_LTLIBRARIES)
 
@@ -144,7 +144,7 @@
 
 install-libLTLIBRARIES: $(lib_LTLIBRARIES)
 	@$(NORMAL_INSTALL)
-	$(mkinstalldirs) $(DESTDIR)$(libdir)
+#	$(mkinstalldirs) $(DESTDIR)$(libdir)
 	@list='$(lib_LTLIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    echo "$(LIBTOOL)  --mode=install $(INSTALL) $$p $(DESTDIR)$(libdir)/$$p"; \
@@ -298,7 +298,7 @@
 install-strip:
 	$(MAKE) $(AM_MAKEFLAGS) AM_INSTALL_PROGRAM_FLAGS=-s install
 installdirs:
-	$(mkinstalldirs)  $(DESTDIR)$(libdir)
+#	$(mkinstalldirs)  $(DESTDIR)$(libdir)
 
 
 mostlyclean-generic:
diff -urN midgard-lib-1.4.1_5/test/Makefile.in midgard-lib-1.4.1_5.grzegorz/test/Makefile.in
--- midgard-lib-1.4.1_5/test/Makefile.in	Sat Apr 14 16:43:31 2001
+++ midgard-lib-1.4.1_5.grzegorz/test/Makefile.in	Wed Jun 20 12:55:49 2001
@@ -87,7 +87,7 @@
 LDADD = $(top_srcdir)/src/libmidgard.la
 
 INCLUDES = -I$(top_srcdir)
-mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
+#mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_CLEAN_FILES = 
 PROGRAMS =  $(noinst_PROGRAMS)
 
